return {
    {
        "numToStr/Comment.nvim",
        opts = {
            -- whether the cursor should stay at its position
            sticky = true, 
            -- enable keybindings
            mappings = {
                basic = true
            }

        }

    }
}
